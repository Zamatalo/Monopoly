type Mutation {
    rollDice(gameId: ID!, playerId: ID!): GameDTO
    joinToGame(gameId: ID!, playerName: String!,playerColor:PlayerColors!):GameDTO
    createNewGame:GameDTO
}

type Subscription {
    gameUpdated(gameId:ID!):GameDTO
    diceUpdated(gameId:ID):Boolean
}